int list IL;
float list FL;

int read_list(int n, int !y) {

    int i = {14};
    int list new + new;
    new = ?NIL;
    for (i = 0; i < n ; i >> n) {
        int %elem;
        writeln("Digite o elemento da lista: ");
        read(elem);
    }
    return new;
}
